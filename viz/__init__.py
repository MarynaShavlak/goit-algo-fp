"""Пакет візуалізації для goit-algo-fp.

Тут зібрано весь код малювання (turtle / matplotlib / networkx), щоб `main.py`
кожної задачі лишався чистим — лише алгоритм і демонстрація. Модулі:

  * pythagoras     — рендер дерева Піфагора (turtle + PNG)        → task_2
  * dijkstra_graph — малювання зваженого графа з деревом шляхів    → task_3
  * binary_tree    — Node + малювання бінарного дерева             → task_4, task_5
  * dice_chart     — стовпчиковий графік Монте-Карло vs аналітика  → task_7
  * colors         — lerp_color: спільний градієнт кольорів        → task_2, task_5

Кожна функція з аргументом `save_path` зберігає PNG (backend Agg, без дисплея),
а без нього — показує інтерактивне вікно.
"""
